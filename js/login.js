// login.js
console.log("main.js está sendo executado!");
/*
function validarForm() {
    var email = document.getElementById("email").value;
    var senha = document.getElementById("senha").value;

    // Adicione aqui a lógica de validação (pode ser feita em JavaScript ou no lado do servidor)

    // Exemplo simples: verifica se os campos não estão vazios
    if (email === "" || senha === "") {
        alert("Preencha todos os campos");
        return false;
    }

    // Aqui você pode adicionar lógica adicional, como enviar os dados para o servidor
   
    return true;
}*/
